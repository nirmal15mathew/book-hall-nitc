import { json } from "@sveltejs/kit";
import { getSchedule } from "$lib/server/db";

export function GET() {
    // get all the schedules for the
    // particular hall code and time interval
    const {}
    return json();
}